<?xml version="1.0" encoding="utf-8" ?>
<views:BaseContentPage xmlns:views="clr-namespace:GoEng.Views"
                       xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:controls="clr-namespace:GoEng.Controls" 
                       xmlns:selectors="clr-namespace:GoEng.TemplateSelectors"
                       x:Class="GoEng.Views.Home.GameVariantView">
    <StackLayout>

        <controls:HomeDetailsNavBar />

        <!--<CarouselView
                HorizontalOptions="Center"
                HorizontalScrollBarVisibility="Never"
                IsSwipeEnabled="False"
                ItemsSource="{Binding TabItems}"
                Position="{Binding CurrentGameVariant}"
                VerticalOptions="Center"
                VerticalScrollBarVisibility="Never">

            <CarouselView.ItemTemplate>


            </CarouselView.ItemTemplate>

        </CarouselView>-->
        
    </StackLayout>

</views:BaseContentPage>